# 需求1：尝试只读打开文件text，txt文件存在读取内容，不存在通知用户
# 需求2：读取内容 循环读取 并在循环结束后退出 如果循环意外终止，提示用户终止
import time
try:
    f = open('text.txt')
    try:
        while True:
            con = f.readline()
            if len(con) == '0':
                break
            time.sleep(3)
            print(con)
    except:
        print('程序已经终止')


except:
    print('文件不存在')
